/*
	MODELS NAMESPACE
 */
window.locationshare = (function setUpModels (module) {
	module.models = {
		Client: Backbone.Model.extend({
			initialize: function () {

			}
		})
	};
	return module;
}(window.locationshare || {}));